.langOverlay {
    max-width: 100%;
}

.langButton {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0;
    border: 0;
    background: none;
    cursor: pointer;
    color: inherit;
    font: inherit;
}

.langLabel {
    margin: 0 8px;
}
