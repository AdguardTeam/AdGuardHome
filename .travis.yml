language: go
sudo: false
go:
  - 1.10.x
  - 1.11.x
  - master
  - tip

os:
  - linux
  - osx

script:
  - make
  - cd dnsfilter && go get -d -t . && go test
  - cd ../coredns_plugin && go get -d -t . && go test
  - cd .. && make

