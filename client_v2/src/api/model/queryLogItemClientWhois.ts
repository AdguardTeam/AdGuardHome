/**
 * Client WHOIS information, if any.
 */
export interface QueryLogItemClientWhois {
    /** City, if any. */
    city?: string;
    /** Country, if any. */
    country?: string;
    /** Organization name, if any. */
    orgname?: string;
}
