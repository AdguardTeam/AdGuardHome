.nameCell {
    max-width: 420px;
}

.nameCellLink {
    color: inherit;
    text-decoration: none;
    min-width: 0;

    &:hover,
    &:focus {
        color: inherit;
        text-decoration: underline;
    }
}
