.container {
    position: relative;
    display: inline-flex;
    cursor: pointer;
    align-items: center;
    gap: 8px;
    padding: 4px 8px;
    background: var(--default-input-background);
    border: 1px solid var(--default-inactive-input-stroke);
    border-radius: 4px;
    transition: var(--t2) border-color, var(--t2) background-color;
    width: fit-content;
}

.container {
    &:hover {
        border-color: var(--default-active-input-stroke);
        background: var(--hovered-page-background);
    }

    &:hover .icon {
        color: var(--default-main-text);
        transform: scale(1.1);
    }

    &:focus {
        border-color: var(--default-active-input-stroke);
    }

    &:focus .icon {
        color: var(--default-main-text);
    }

    &:active .icon {
        transform: scale(0.95);
    }
}

.text {
    font-size: 14px;
    color: var(--default-main-text);
    cursor: pointer;
    user-select: none;
    word-break: break-all;
    line-height: 1.4;
}

.icon {
    width: 20px;
    height: 20px;
    color: var(--default-gray-icons);
    cursor: pointer;
    transition: var(--t2) color, var(--t2) transform;
    flex-shrink: 0;
    pointer-events: none;
}

.copied {
    color: var(--product-primary-50) !important;
    transform: scale(1.1) !important;
}
