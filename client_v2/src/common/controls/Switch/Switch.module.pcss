.switch {
    display: inline-flex;
    align-items: center;
    cursor: pointer;
    gap: 8px;

    &.disabled {
        cursor: default;
    }
}

.input {
    /* Hide the checkbox without removing it from the accessibility tree or the
       tab order, unlike display: none. */
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    overflow: hidden;
    clip-path: inset(50%);
    white-space: nowrap;
    border: 0;
}

.input:focus-visible + .handler {
    outline: 2px solid var(--stroke-focus, currentcolor);
    outline-offset: 2px;
}

.label {
    font-size: 16px;
    font-weight: var(--weight-medium);
    user-select: none;
}

.handler {
    position: relative;
    width: 40px;
    height: 20px;
    border-radius: 24px;
    background-color: var(--fills-switch-off-default);
    transition: background-color var(--t2);

    &:hover,
    &:focus {
        background-color: var(--fills-switch-off-hovered);
    }

    &:before {
        content: '';
        position: absolute;
        top: 2px;
        left: 2px;
        width: 16px;
        height: 16px;
        border-radius: 16px;
        background-color: var(--fills-switch-knob);
        transition:
            transform var(--t2),
            background-color var(--t2);
    }
}

.input:checked + .handler {
    background-color: var(--fills-switch-on-default);

    &:hover,
    &:focus {
        background-color: var(--fills-switch-on-hovered);
    }

    &:before {
        transform: translateX(20px);
    }
}

.input:disabled + .handler {
    cursor: default;
    background-color: var(--fills-switch-off-disabled);

    &:hover,
    &:focus {
        background-color: var(--fills-switch-off-disabled);
    }

    &:before {
        color: var(--fills-switch-knob-disabled);
    }
}

.input:checked:disabled + .handler {
    background-color: var(--fills-switch-on-disabled);

    &:hover,
    &:focus {
        background-color: var(--fills-switch-on-disabled);
    }

    &:before {
        color: var(--fills-switch-knob-disabled);
    }
}
